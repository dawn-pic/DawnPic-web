{
  "name": "dawn-pic-front-end",
  "version": "1.2.2",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "release": "next lint && changelogen --release --minor && git push --follow-tags",
    "release:patch": "next lint && changelogen --release --patch && git push --follow-tags",
    "release:minor": "next lint && changelogen --release --minor && git push --follow-tags",
    "release:major": "next lint && changelogen --release --major && git push --follow-tags"
  },
  "dependencies": {
    "@iconify/react": "^5.0.2",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.487.0",
    "next": "14.2.5",
    "react": "^18",
    "react-dom": "^18",
    "react-paginate": "^8.2.0",
    "tailwind-merge": "^3.1.0",
    "tw-animate-css": "^1.2.5"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.3",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.2.5",
    "postcss": "^8",
    "tailwindcss": "^4.1.3",
    "typescript": "^5"
  }
}
